<?xml version='1.0' encoding='utf-8'?>
<InstructionSet name="Z80">
<Register name="pc" description="The program counter holds the 16-bit address of the current instruction being fetched from memory. The PC is automatically incremented after its contents have been transferred to the address lines."
    type="Special Purpose Register" width="16 bits">
    </Register>
    <Register name="sp" description="The stack pointer holds the 16-bit address of the current top of a stack located anywhere in external system RAM memory."
    type="Special Purpose Register" width="16 bits">
    </Register>
    <Register name="ix" description="16-bit index or base register for 8-bit immediate offsets used in index addressing modes."
    type="Special Purpose Register" width="16 bits">
    </Register>
    <Register name="iy" description="16-bit index or base register for 8-bit immediate offsets used in index addressing modes."
    type="Special Purpose Register" width="16 bits">
    </Register>
    <Register name="i" description="Interrupt vector base register."
    type="Special Purpose Register" width="8 bits">
    </Register>
    <Register name="r" description="DRAM refresh counter, 8 bits (msb does not count)."
    type="Special Purpose Register" width="8 bits">
    </Register>
    <Register name="a" description="Accumulator."
    width="8 bits">
    </Register>
    <Register name="a'" description="Alternate (or shadow) accumulator."
    width="8 bits">
    </Register>
    <Register name="f" description="The flag register. Holds bits to indicate carry, zero, minus, parity/overflow, half-carry (used for BCD), and an Add/Subtract flag (usually called N) also for BCD."
    width="8 bits">
    </Register>
    <Register name="f'" description="Alternate (or shadow) flag register. Holds bits to indicate carry, zero, minus, parity/overflow, half-carry (used for BCD), and an Add/Subtract flag (usually called N) also for BCD."
    width="8 bits">
    </Register>
    <Register name="af" description="8-bit accumulator (A) and flag bits (F) carry, zero, minus, parity/overflow, half-carry (used for BCD), and an Add/Subtract flag (usually called N) also for BCD."
    width="16 bits">
    </Register>
    <Register name="af'" description="Alternate (or shadow) 8-bit accumulator (A) and flag bits (F) carry, zero, minus, parity/overflow, half-carry (used for BCD), and an Add/Subtract flag (usually called N) also for BCD."
    width="16 bits">
    </Register>
    <Register name="b" description="General purpose register."
    type="General Purpose Register" width="8 bits">
    </Register>
    <Register name="b'" description="Alternate (or shadow) general purpose register."
    type="General Purpose Register" width="8 bits">
    </Register>
    <Register name="c" description="General purpose register."
    type="General Purpose Register" width="8 bits">
    </Register>
    <Register name="c'" description="Alternate (or shadow) general purpose register."
    type="General Purpose Register" width="8 bits">
    </Register>
    <Register name="bc" description="16-bit data/address register or two 8-bit registers."
    width="16 bits">
    </Register>
    <Register name="bc'" description="Alternate (or shadow) 16-bit data/address register or two 8-bit registers."
    width="16 bits">
    </Register>
    <Register name="d" description="General purpose register."
    type="General Purpose Register" width="8 bits">
    </Register>
    <Register name="d'" description="Alternate (or shadow) general purpose register."
    type="General Purpose Register" width="8 bits">
    </Register>
    <Register name="e" description="General purpose register."
    type="General Purpose Register" width="8 bits">
    </Register>
    <Register name="e'" description="Alternate (or shadow) general purpose register."
    type="General Purpose Register" width="8 bits">
    </Register>
    <Register name="de" description="16-bit data/address register or two 8-bit registers."
    width="16 bits">
    </Register>
    <Register name="de'" description="Alternate (or shadow) 16-bit data/address register or two 8-bit registers."
    width="16 bits">
    </Register>
    <Register name="h" description="General purpose register."
    type="General Purpose Register" width="8 bits">
    </Register>
    <Register name="h'" description="Alternate (or shadow) general purpose register."
    type="General Purpose Register" width="8 bits">
    </Register>
    <Register name="l" description="General purpose register."
    type="General Purpose Register" width="8 bits">
    </Register>
    <Register name="l'" description="Alternate (or shadow) general purpose register."
    type="General Purpose Register" width="8 bits">
    </Register>
    <Register name="hl" description="16-bit accumulator/address register or two 8-bit registers."
    width="16 bits">
    </Register>
    <Register name="hl'" description="Alternate (or shadow) 16-bit accumulator/address register or two 8-bit registers."
    width="16 bits">
    </Register>
</InstructionSet>
